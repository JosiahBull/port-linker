/target

# Test SSH keys
tests/docker/test_key
tests/docker/test_key.pub

# cargo-mutants
mutants.out
mutants.out.old

# Mermaid cache
.mermaid/

# Pre-built agents staged for the CLI build (CI writes this)
/prebuilt

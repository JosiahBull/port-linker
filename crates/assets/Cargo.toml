[package]
name = "assets"
version.workspace = true
edition.workspace = true
description = "Logo and asset generation for port-linker"

[dependencies]

[build-dependencies]
resvg = { workspace = true }
